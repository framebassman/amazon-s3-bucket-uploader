export interface Input {
    bucketName: string;
    endpoint: string;
    region: string;
    accessKeyId: string;
    secretAccessKey: string;
    actorRunId: string;
    pathName: string;
    fileName: string;
    separateItems: boolean;
}
